import styles from '../styles/footer.module.css'

const Footer = (props) => {

  return (
    <div className={styles.footer}>
      nextjs pre-rendering / client side rendering
    </div>
  );
};

export default Footer
